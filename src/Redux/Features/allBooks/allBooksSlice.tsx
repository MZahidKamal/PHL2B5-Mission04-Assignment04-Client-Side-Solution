/*
import {createSlice} from "@reduxjs/toolkit";


interface AllBooksInterface {
    allBooks: [];
}


const initialState = []


const AllBooksSlice = createSlice({
    name: "allBooks",
    initialState,
    reducers: {
        getAllBooks: (state) => {
            state.allBooks = [];
        }
    }
})*/
